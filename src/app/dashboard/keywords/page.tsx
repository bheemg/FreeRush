import { Placeholder } from "@/components/dashboard/placeholder";

export default function KeywordResearchPage() {
  return (
    <Placeholder title="Keyword Research" phase="Phase 2">
      A workspace-wide keyword engine using free sources — Google Autocomplete for discovery, People-Also-Ask and
      Related searches for expansion, and Google Trends for relative demand bands. The provider layer is already
      wired (<code>KeywordProvider</code>); this view surfaces it next.
    </Placeholder>
  );
}
