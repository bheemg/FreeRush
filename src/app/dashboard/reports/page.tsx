import { Placeholder } from "@/components/dashboard/placeholder";

export default function ReportsPage() {
  return (
    <Placeholder title="Reports" phase="Phase 1">
      Saved analyses and the long-form Growth Report (the 4-stage marketing chain) will live here, each rendered to a
      shareable, downloadable PDF. The background-job infrastructure (pg-boss) that runs these without serverless
      timeouts is already in place.
    </Placeholder>
  );
}
